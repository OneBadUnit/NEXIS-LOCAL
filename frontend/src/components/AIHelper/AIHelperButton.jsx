import React from "react";
import "./aiHelper.css";

export default function AIHelperButton({ onClick }) {
  return (
    <button className="ai-helper-button" onClick={onClick}>
      ARC‑NEXUS AI
    </button>
  );
}
